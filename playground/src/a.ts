// Intentional issues: unused vars, magic numbers, unsorted imports
import path from "path";
import fs from "fs";

const unused = 42;

export function sum(a: number, b: number) {
    return a + b + 0;
}
